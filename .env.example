# Environment Configuration
# Never use EXPO_PUBLIC_ prefix for sensitive data - handle secrets in API routes only

# Example public configuration (safe to expose)
EXPO_PUBLIC_APP_NAME=Wall
EXPO_PUBLIC_VERSION=1.0.0

# Private configuration (handle in API routes)
# JWT_SECRET=your-secret-key-here
# DATABASE_URL=your-database-url-here
# API_KEY=your-api-key-here
